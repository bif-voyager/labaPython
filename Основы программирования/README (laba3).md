
# Лабораторная работа №3

## Задание

1. Реализовать функцию для линеаризации вложенных списков двумя способами:
- рекурсивно;
- итеративно (без рекурсии).

	Пример:  
	linearize([1, 2, [3, 4, [5, [6, []]]]]) → [1, 2, 3, 4, 5, 6]

2. Реализовать функции для вычисления последовательностей  
    aₖ = 2·bₖ₋₁ + aₖ₋₁  
    bₖ = 2·aₖ₋₁ + bₖ₋₁  
    
    при условиях a₁ = b₁ = 1,  
    
    двумя способами:
    - рекурсивно;
    - итеративно.
## Описание проделанной работы

Были разработаны две группы функций.
### 1. Линеаризация списков

**Рекурсивный вариант:**  
Функция последовательно обходит элементы списка.  
Если элемент — простой объект, он добавляется в новый список.  
Если элемент — список, вызывается функция для вложенного списка.  
Таким образом происходит естественное раскрытие любой глубины.

**Итеративный вариант:**  
Для обхода используется собственный стек.  
В него помещаются элементы исходного списка.  
Если стек получает вложенный список — его элементы добавляются обратно в стек в обратном порядке.  
Это позволяет сохранить порядок элементов без рекурсии.

Обе версии дают идентичный результат.

---
### 2. Вычисление последовательностей aₖ и bₖ

**Рекурсивный вариант:**  
Реализовано строго по рекуррентным формулам:  
сначала вычисляются значения для k−1, затем — aₖ и bₖ.  
База — k = 1.

**Итеративный вариант:**  
Используется простой цикл.  
Начинаем с (a₁, b₁), затем последовательно пересчитываем значения до нужного k.  
Метод полностью избегает рекурсии и гарантированно не вызывает переполнения стека.

---
## Код программы

### Линеаризация списков

Рекурсивная версия:

```python
def linearize_recursive(xs):
    result = []
    for item in xs:
        if isinstance(item, list):
            result.extend(linearize_recursive(item))
        else:
            result.append(item)
    return result
```

Итеративная версия:

```python
def linearize_iterative(xs):
    result = []
    stack = [xs]
    while stack:
        current = stack.pop()
        if isinstance(current, list):
            for elem in reversed(current):
                stack.append(elem)
        else:
            result.append(current)
    return result
```

---
### Вычисление последовательностей

Рекурсивная версия:

```python
def ab_recursive(k):
    if k == 1:
        return 1, 1
    a_prev, b_prev = ab_recursive(k - 1)
    return 2*b_prev + a_prev, 2*a_prev + b_prev
```

Итеративная версия:

```python
def ab_iterative(k):
    a, b = 1, 1
    for _ in range(2, k + 1):
        a, b = 2*b + a, 2*a + b
    return a, b
```

---
## Скриншоты результатов

<img width="699" height="422" alt="image" src="https://github.com/user-attachments/assets/cfa581ba-a284-4bef-8ab5-6040f6a45df8" />


---

## Результаты вычислений

Исходный список:  
`[1, 2, [3, 4, [5, [6, []]]]]`

Рекурсивная линеаризация:  
`[1, 2, 3, 4, 5, 6]`

Итеративная линеаризация:  
`[1, 2, 3, 4, 5, 6]`

Последовательности:

- k = 1 → a = 1, b = 1
- k = 2 → a = 3, b = 3
- k = 3 → a = 9, b = 9
- k = 4 → a = 27, b = 27
- k = 5 → a = 81, b = 81
- k = 6 → a = 243, b = 243
- k = 7 → a = 729, b = 729

---
## Вывод

В ходе лабораторной работы были изучены и реализованы рекурсивные и нерекурсивные алгоритмы.  
Рекурсивная реализация проще и короче, но может быть ограничена глубиной стека.  
Итеративная версия более надёжная для больших данных.  
Во всех задачах обе реализации дали одинаковые результаты, что подтверждает правильность алгоритмов.

---

## Список использованных источников

1. [Recursion in Programming - Full Course - freeCodeCamp.org](https://youtu.be/IJDJ0kBx2LM)
2. [🐍 Самоучитель по Python для начинающих. Часть 13: Рекурсивные функции - proglib.io](https://proglib.io/p/samouchitel-po-python-dlya-nachinayushchih-chast-13-rekursivnye-funkcii-2023-01-23)
3. [Как работает рекурсия – объяснение в блок-схемах и видео - Хабр](https://habr.com/ru/articles/337030/)
